# LLM generated this file 

# Environment for RAG project (tested on CUDA 12.8 driver)
# Python 3.11
#
# STEP 1 — Install PyTorch (cu126, compatible with CUDA 12.8):
#   pip install torch==2.7.0+cu126 torchaudio==2.7.0+cu126 torchvision==0.22.0+cu126 \
#       --index-url https://download.pytorch.org/whl/cu126
#
# STEP 2 — Install vLLM (cu126 wheel, last version with cu126 prebuilt):
#   pip install https://github.com/vllm-project/vllm/releases/download/v0.9.1/vllm-0.9.1%2Bcu126-cp38-abi3-manylinux1_x86_64.whl \
#       --extra-index-url https://download.pytorch.org/whl/cu126
#
# STEP 3 — Install everything below:
#   pip install -r requirements.txt
#
# STEP 4 — Patch vLLM's ovis.py to allow aimv2 re-registration (transformers 4.x conflict):
#   FILE: venv/lib/python3.11/site-packages/vllm/transformers_utils/configs/ovis.py
#   CHANGE: AutoConfig.register("aimv2", AIMv2Config)
#       TO: AutoConfig.register("aimv2", AIMv2Config, exist_ok=True)
#
# STEP 5 — Serve the model:
#   vllm serve Qwen/Qwen3-8B-AWQ --port 8002 --max-model-len 2048 \
#       --gpu-memory-utilization 0.5 --enable-auto-tool-choice --tool-call-parser hermes

# Core ML
torch==2.7.0+cu126
torchvision==0.22.0+cu126
torchaudio==2.7.0+cu126
transformers==4.57.6
tokenizers==0.22.2
safetensors==0.7.0
huggingface_hub==0.36.2
accelerate

# vLLM (installed separately — see STEP 2 above)
# vllm==0.9.1+cu126

# RAG / retrieval
sentence-transformers==5.5.1
faiss-cpu==1.14.2
bm25s==0.3.9

# OpenAI client
openai==2.41.0
python-dotenv==1.2.2

# Numerics
numpy==2.2.6
scipy==1.17.1
numba==0.61.2

# vLLM runtime dependencies
aiohttp==3.14.0
blake3==1.0.8
cachetools==7.1.4
cloudpickle==3.1.2
compressed-tensors==0.10.1
depyf==0.18.0
einops==0.8.2
fastapi==0.136.3
filelock==3.29.1
lark==1.2.2
mistral_common==1.11.3
msgspec==0.21.1
ninja==1.13.0
outlines==0.1.11
packaging==26.2
pillow==12.2.0
protobuf==6.33.6
psutil==7.2.2
pydantic==2.13.4
pyzmq==27.1.0
ray==2.55.1
regex==2026.5.9
requests==2.34.2
sentencepiece==0.2.1
tqdm==4.68.1
triton==3.3.0
uvicorn==0.49.0
xformers==0.0.30
xgrammar==0.1.19
