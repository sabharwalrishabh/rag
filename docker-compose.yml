services:
  rag-api:
    image: sabharwalrishabh/rag-api:latest
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - rag-cache:/app/cache
      - hf-cache:/root/.cache/huggingface
    restart: unless-stopped

volumes:
  rag-cache:
  hf-cache:
